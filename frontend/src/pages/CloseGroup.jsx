import { despesasModule } from '../services/despesasService';

const CloseGroup = () => {
    const { expenses, toggleLike, addExpense } = despesasModule(null, null, groupId, members);

    return "COMPOENNT AINDA NÂO FINALIZADO";
}